import 'package:flutter/material.dart';

class DesignInputField extends StatelessWidget {
  const DesignInputField({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
